name: ssb-room
image: debian-10-x64
min_size: 1gb
config:
  #cloud-config
  packages:
    - wget
  runcmd:
    - wget https://raw.githubusercontent.com/leo-lb/ssb-room/improve-security/install.sh
    - bash install.sh
